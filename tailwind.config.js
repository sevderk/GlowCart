/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      // we can add brand colors, fonts, spacing etc. here later
    },
  },
  plugins: [],
};
